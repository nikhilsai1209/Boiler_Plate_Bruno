# Bruno API Testing Boilerplate

## Installation

To set up Bruno for API testing, follow these steps:

### Step 1: Install Bruno CLI Globally

Run the following command to install the Bruno CLI:

```bash
npm install -g brunocli

